# Zeabur API Configuration
ZEABUR_API_KEY=your_zeabur_api_key_here
ZEABUR_SERVICE_ID=your_service_id_here
ZEABUR_ENV_ID=your_environment_id_here

# Vault Hub API Configuration
VAULT_HUB_BASE_URL=your_vault_hub_base_url_here
VAULT_HUB_API_KEY=your_vault_hub_api_key_here
VAULT_HUB_NAME=your_vault_name_here

# Optional: Cron Schedule (if not set, runs once and exits)
# Format: second minute hour day month day-of-week
# Presets: @every_second, @every_minute, @hourly, @daily, @weekly, @monthly, @yearly
# Custom: @every_5_minutes, @every_30_seconds, etc.
# CRON_SCHEDULE=@every_5_minutes
